import { Stack, Text } from "@mantine/core";

type Props = {
  start: boolean;
  end: boolean;
}

function ContractCard({ start, end }: Props) {
  return (
  <Stack>
      <Text>{}</Text>
  </Stack>
  )
}

export default ContractCard;
